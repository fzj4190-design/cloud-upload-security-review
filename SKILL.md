---
name: cloud-upload-security-review
description: 对拟上传、推送、发布、部署、同步或共享到 GitHub、Sites、对象存储、网盘、邮件附件及其他云端的 Skill、源码、构建产物、文档、归档、日志和数据执行上传前安全审核，并输出 PASS 或 BLOCK 门禁。Use when files may leave the local machine, when a cloud copy may already be downloadable, or when cloud visibility and leakage must be checked. Do not use for local-only reading or editing with no outbound transfer, and do not treat a PASS as authorization to upload.
---

# 云端上传安全审核

## Provenance

本 Skill 由 OpenAI Codex（AI）在用户授权下整理生成，依据公开网络安全案例、标准和厂商/项目公告形成规则；它不是第三方认证，也不替代专业渗透测试、法律意见或云厂商安全评估。研究来源、访问日期和适用限制见 [SOURCES.md](SOURCES.md)。

## 目标

在任何文件离开本机前建立失败关闭的安全门禁。唯一主要结果是针对“精确内容 + 精确目的地 + 精确权限”签发 `PASS`，或签发 `BLOCK` 并阻止上传。

`PASS` 只说明本次审核通过，不代表用户已授权上传、发布、发邮件、删除云端副本、吊销密钥或修改权限。

## 强制规则

- 在上传、`git push`、发布 Release、部署站点、同步网盘、发送附件或调用任何可能外传文件的工具前运行本 Skill。
- 只要范围、内容、历史、构建产物、目的地、读者权限或扫描覆盖无法核实，就输出 `BLOCK`。不得用“应该是私有”代替实时证据。
- 不在输出中显示密钥、令牌、密码、Cookie、身份证号、手机号、邮箱正文或其他隐私原文。只报告类别、相对路径、行号或归档成员、严重度及不可逆指纹前缀。
- 审核默认只读。修复、删除云端文件、清理 Git 历史、撤销共享、轮换密钥和实际上传都需要与审核分开的明确授权。
- 内容、提交、构建、上传清单、目的地或权限发生任何变化后，旧 `PASS` 立即失效。

## 工作流

### 1. 固定审核对象

先记录：

1. 要离开本机的精确文件清单；不能只写“整个项目”。
2. 目标服务、仓库/桶/站点/收件人、目标路径和预计可见性。
3. 当前文件哈希；Git 项目还要记录提交 SHA、分支、标签和工作区状态。
4. 用户请求的是“仅审核”还是同时明确授权了后续上传。没有后者时，完成审核后停止。

### 2. 执行本地确定性扫描

对精确上传清单运行：

```powershell
python scripts/preflight_scan.py <path> [<path> ...] --json
```

脚本会扫描隐藏文件、文本、常见归档和 Office Open XML，检查密钥特征、凭据赋值、敏感文件名、有效中国身份证号、手机号、邮箱、本机用户路径、持仓/成本等业务数据、归档穿越和覆盖缺口。

- 退出码 `0`：脚本层未发现阻断项；仍须完成下面的历史、云权限、依赖和构建检查。
- 退出码 `1`：发现待核实或敏感项，保持 `BLOCK`，逐项确认是假阳性或完成整改。
- 退出码 `2`：扫描不完整、文件过大、加密/损坏归档或运行错误，直接 `BLOCK`。

脚本不扫描 Git 对象数据库，也不能证明云端权限。不要把脚本的 `PASS` 当作最终门禁。

### 3. 扩展到所有可能上传的内容

按适用性检查：

- 当前源码，以及隐藏、忽略、未跟踪文件；核对实际上传工具会包含哪些文件。
- 所有可达 Git 历史、分支、标签、提交差异和已删除文件；私有仓库的读者仍可取得完整历史。
- 构建目录、客户端 JavaScript、source map、移动端包、容器层、缓存、测试夹具和本地回退数据。
- Release、Actions/CI 制品与日志、Pages/Sites 静态资源、备份、数据库导出、临时分享链接和邮件附件。
- PDF、图片、音视频、数据库及脚本无法完整解析的二进制，使用相应解析/元数据工具复核；无法复核则保持 `BLOCK`。

重点反查 `.env`、私钥、云密钥、API Key、SMTP 授权码、URL 中的 bearer token、真实姓名、有效身份证、手机号、邮箱、聊天记录、持仓、成本、策略阈值和绝对用户路径。真实数据不得作为客户端 fallback、示例或测试夹具编入公开/可共享产物。

### 4. 实时核查云端可下载性

必须从目标服务取得当前证据，而不是依赖本地 remote 或旧截图：

- 仓库/对象/站点的 public/private 状态、协作者、团队、外部访客、待处理邀请、fork、部署、Pages、Release、制品和分享链接。
- 匿名访问，以及最低权限普通读者是否能下载源码、历史、静态资源、日志或制品。
- CI 制品保留期、对象存储 ACL、目录继承、搜索引擎可索引性和静态资源是否绕过应用层鉴权。
- 邮件或消息附件的真实收件人、抄送、群组展开结果和外部域。

若连接器不可用、认证失败、目标 URL 未知或无法做匿名/最低权限验证，最终结果必须是 `BLOCK: CLOUD_VISIBILITY_UNVERIFIED`。

### 5. 审核依赖和自动化权限

- 对锁文件运行生态对应的审计，例如 `npm audit --json`、`pip-audit` 或 OSV 精确版本查询。高危/严重漏洞未修复时阻止发布。
- 检查包名幻觉、依赖混淆、未锁版本和缺失哈希；安装阶段可执行第三方代码，不能在同一高权限作业中暴露整组密钥。
- CI 使用最小 `GITHUB_TOKEN`/云权限；外部 Action 固定完整提交 SHA；避免把不可信输入直接插值进 shell；仅在必要步骤提供凭据。
- 若安全扫描功能因套餐或权限不可用，不得把“接口无结果”解释为“没有告警”。

### 6. 验证程序并重扫产物

运行适用的单元测试、静态检查和生产构建。测试成功只证明功能行为，不抵消安全发现。生成最终产物后再次运行本地扫描，并确认产物哈希与拟上传清单一致。

### 7. 签发门禁

完整判定规则和报告格式见 [references/gate-policy.md](references/gate-policy.md)。必须满足：

- 无未解决的严重、高危或覆盖缺口。
- 中危项已整改；只有用户明确接受且存在具体补偿控制时才可例外，并在报告中写明。
- 云端权限已实时验证且符合预期。
- 程序测试、依赖审计、构建后复扫均完成。
- `PASS` 绑定审核 ID、内容哈希/提交、目的地、可见性和 24 小时有效期。

否则输出 `BLOCK`。不得以免责声明代替阻止。

## 已上传内容的处理

若发现云端已经存在敏感内容：

1. 先报告已确认事实、下载权限和证据时间，不回显敏感值。
2. 对确认泄露的密钥建议先撤销/轮换，再清理当前文件、历史、缓存、制品和镜像；仅在用户明确授权后执行。
3. 重新检查匿名访问、最低权限下载和搜索索引，并重新签发门禁。
4. 无法证明历史副本已不可用时，按“曾泄露”处理，不能只删除当前文件了事。

## Gotchas

- 私有仓库不是无泄露：协作者可下载源码和历史，CI 依赖也可能接触作业级密钥。
- 应用 API 返回 404 不代表静态资源受保护；单独检查客户端 bundle 和平台静态资源路由。
- 日志脱敏不是绝对保证；扫描实际日志和制品，不依赖平台承诺。
- 密码保护或加密文件若无法安全解密检查，保持 `BLOCK`，不要猜测内容安全。
- 安全审核通过后仍须确认用户确实授权了上传动作。

## 资源

- `scripts/preflight_scan.py`：本地、只读、无第三方依赖的预扫描器；支持 `--self-test`。
- `references/gate-policy.md`：严重度、失败关闭规则、证据要求和最终报告模板。
- `SOURCES.md`：公开来源、案例归属、研究日期和不确定性说明。
